export interface PostMetadata {
  title: string;
  image: string;
  date: string;
  subtitle: string;
  slug: string;
  category: string;
  author: string;
}

//export default const getNewsMetadata = (): PostMetadata[] => {
//};